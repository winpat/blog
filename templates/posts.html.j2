{% extends "layout.html.j2" %}

{% block content %}
  {% for post in posts %}
	<article id="post">
	  <h3>
		<a href="{{ POSTS_DIR / post.file_name }}">{{ post.title }}</a>
	  </h3>
	  <p id="date">
		{{ post.publish_date.strftime('%A, %d %B, %Y') }}
	  </p>
	</article>
  {% endfor %}
{% endblock %}
